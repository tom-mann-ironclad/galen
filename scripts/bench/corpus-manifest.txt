# Pinned public repositories checked out to build the ClamAV-comparison
# benchmark corpus. Each line is: <git-url> <commit-sha> <subdir-name>
#
# Commits are pinned (not branch/tag names) so the corpus - and therefore
# the benchmark numbers - stay reproducible across nightly runs until this
# file is deliberately updated. SHAs were verified with:
#   git ls-remote --tags <git-url>
#
# This is a deliberately modest starting corpus (~45k files, a few hundred
# MB of real, mixed-language source and binary fixtures) chosen to keep
# nightly CI time and bandwidth reasonable. Add more lines to scale it up.

https://github.com/python/cpython.git fdb81425a9ad683f8c24bf5cbedc9b96baf00cd2 cpython
https://github.com/nodejs/node.git 4631be0311fbde7b77723757e15d025727399e63 node
